// Auth Controller Placeholders

const registerUser = async (req, res) => {
  res.status(501).json({ message: "Registration feature not implemented yet." });
};

const loginUser = async (req, res) => {
  res.status(501).json({ message: "Login feature not implemented yet." });
};

module.exports = {
  registerUser,
  loginUser,
};
